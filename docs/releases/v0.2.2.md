## Highlights
- `tokimeter savings` — routine-looking premium turns that could run on a cheaper same-provider tier (upper bound, never a recommendation)
- `tokimeter burn` — runaway-agent alarm: burn-rate spikes vs your own baseline
- `tokimeter trace` and `tokimeter compare`
- Report export: `--json`, `--md`, `--html`
- `savings --emit-policy` — LiteLLM/OpenRouter routing policy from your real usage
